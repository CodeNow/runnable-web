<button type="button"></button>
<select>
{{#each collection.models}}
	{{#if attributes.selected}}
		<option selected>{{attributes.name}}
	{{else}}
		<option>{{attributes.name}}
	{{/if}}
{{/each}}
</select>
